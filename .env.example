# ==========================================
# EXPIRY TRACKER - PLANTILLA DE ENTORNO
# ==========================================
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Modifica las contraseñas y secretos por valores seguros.
# 3. NUNCA subas el archivo .env real al repositorio.
# ==========================================

# ==========================================
# INFRAESTRUCTURA Y PUERTOS
# ==========================================

# --- PUERTOS EXTERNOS (HOST) ---
# Puertos expuestos en tu máquina local
EXTERNAL_PORT_API=8080
EXTERNAL_PORT_DASHBOARD=3000
EXTERNAL_PORT_PGADMIN=5050
EXTERNAL_PORT_DB=5432

# --- PUERTOS INTERNOS (CONTAINER) ---
# Puertos donde escuchan los servicios dentro de Docker
INTERNAL_PORT_API=8080
INTERNAL_PORT_DASHBOARD=5173
INTERNAL_PORT_DB=5432
INTERNAL_PORT_PGADMIN=80

# ==========================================
# CONFIGURACIÓN DE SERVICIOS
# ==========================================

# --- BASE DE DATOS (PostgreSQL) ---
DB_NAME=expiry_tracker
DB_USER=expiry_user
DB_PASSWORD=change_me

# --- GESTIÓN (pgAdmin) ---
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=change_me

# --- SEGURIDAD (JWT) ---
# Generar una cadena larga (ej. openssl rand -base64 32)
JWT_SECRET=insert_a_very_secure_secret_key_here_at_least_32_chars
JWT_EXPIRATION_DAYS=7

# --- NOTIFICACIONES (Telegram) ---
# Obtener del BotFather
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
TELEGRAM_CHAT_ID=000000000

# --- FRONTEND (Vite/Vue) ---
# URL base para conectar con el Backend (desde el navegador)
VITE_API_BASE_URL=http://localhost:${EXTERNAL_PORT_API}/api/v1
